# React-Native/Expo-Pakete deklarieren teils optionale Peers (z.B.
# react-native-windows via @react-native-community/datetimepicker), die npms
# strikte Peer-Auflösung fälschlich als Konflikt wertet. legacy-peer-deps
# umgeht das — in RN/Expo-Projekten üblich und nötig.
legacy-peer-deps=true
